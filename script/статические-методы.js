Array.isArray(); // проверка массива
Number.isNaN(); // Возвращает логическое значение, которое указывает, является ли значение зарезервированным значением NaN (не числом).

function print(a) {
  console.log(a);
}

print("hello world");

const wolf = 'волк'
const work = `работа не ${wolf}, в лес не убежит`;
print(work);